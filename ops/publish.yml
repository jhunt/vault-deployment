---
- type: replace
  path: /instance_groups/name=vault/networks/0/default?
  value: [dns, gateway]

- type: replace
  path: /instance_groups/name=vault/networks/-
  value:
    name: ((public_network))
